{
  "name": "nexlabs-subgraphs-arbitrum-one",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nexlabs-subgraphs-arbitrum-one",
    "create-local": "graph create --node http://localhost:8020/ nexlabs-subgraphs-arbitrum-one",
    "remove-local": "graph remove --node http://localhost:8020/ nexlabs-subgraphs-arbitrum-one",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nexlabs-subgraphs-arbitrum-one",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
